# Web App (Astro)

Frontend for the gold price site.

## Run Locally

From workspace root:

- `pnpm dev:web`

From this folder:

- `pnpm dev`

The app runs on `http://localhost:4321`.

## Build

From workspace root:

- `pnpm --dir apps/web build`

From this folder:

- `pnpm build`
